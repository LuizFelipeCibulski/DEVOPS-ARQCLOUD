apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: targeting-service
spec:
  type: ClusterIP
  selector:
    app: targeting-service
  ports:
  - name: targeting-service
    port: 80
    targetPort: 8003