apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: analytics-service
  name: analytics-service
  namespace: analytics-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      # IRSA: esta SA carrega a anotação com o ARN da role. É o que
      # substitui AWS_ACCESS_KEY_ID/SECRET/SESSION_TOKEN.
      serviceAccountName: analytics-service
      containers:
      - image: 628409561285.dkr.ecr.us-east-1.amazonaws.com/analytics-service:v1.0.0-e926ad6
        name: analytics-service
        env:
         - name: PORT
           valueFrom:
            configMapKeyRef:
              name: analytics-cm
              key: PORT
         - name: AWS_REGION
           valueFrom:
            configMapKeyRef:
              name: analytics-cm
              key: AWS_REGION
         - name: AWS_SQS_URL
           valueFrom:
            configMapKeyRef:
              name: analytics-cm
              key: AWS_SQS_URL
         - name: AWS_DYNAMODB_TABLE
           valueFrom:
            configMapKeyRef:
              name: analytics-cm
              key: AWS_DYNAMODB_TABLE
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        readinessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 10
          periodSeconds: 15
          timeoutSeconds: 3
          failureThreshold: 3 # 3 falhas seguidas = reinicia
